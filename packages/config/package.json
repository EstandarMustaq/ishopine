{
  "name": "@ishopine/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript / tooling config for iShopine apps",
  "files": ["tsconfig.base.json", "tsconfig.next.json"],
  "scripts": {
    "lint": "echo ok"
  }
}
