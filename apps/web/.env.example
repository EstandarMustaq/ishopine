# API NestJS (paths are under /api)
NEXT_PUBLIC_API_URL="http://localhost:4000"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=""

# Optional: public site URL used by OAuth redirects (set on API as WEB_URL)
# WEB_URL="http://localhost:3000"

# Brand: iShopine (product). Company/operator: Nkateko Investment and Service.
# Demo credentials (when API seed uses iShopine emails):
# admin@ishopine.com / IShopine@2026
