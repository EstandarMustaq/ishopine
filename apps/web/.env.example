# API NestJS (paths are under /api)
NEXT_PUBLIC_API_URL="http://localhost:4000"

# Optional: public site URL used by OAuth redirects (set on API as WEB_URL)
# WEB_URL="http://localhost:3000"

# Brand: iShoppine (product). Company/operator: Nkateko Investment and Service.
# Demo credentials (when API seed uses iShoppine emails):
# admin@ishoppine.com / IShoppine@2026
